- [x] Update `src/pages/OrderDetails.jsx` to fall back to fetching from `payments` collection when `orders` doc is not found.
- [x] Update `src/pages/OrderDetails.jsx` UI to show product images, shipping address, per-item selling price, shipping cost, and GST/tax with safe defaults.
- [x] Verify createdAt formatting and route id matching for both `orders` and `payments`.
- [x] Run lint/build (if available) and do a quick manual test of Orders → View Details.



